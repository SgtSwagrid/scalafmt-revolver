name: Linter
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:
  workflow_call:

jobs:
  lint:
    uses: SgtSwagrid/scalafmt-revolver/linter.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
