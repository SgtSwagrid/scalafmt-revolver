## Fixed code style violations with `sbt scalafmtAll`.

Code which doesn't conform this project's [style rules](../.scalafmt.conf) was nevertheless pushed to the default branch.
All violations have been subsequently fixed using `sbt scalafmtAll`.
