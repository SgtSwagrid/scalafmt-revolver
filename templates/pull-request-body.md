## Fixed code style violations with `sbt scalafmtAll`.

The commit [$GITHUB_SHA](https://github.com/$GITHUB_REPOSITORY/commit/$GITHUB_SHA) to the default branch is in violation of the [Scalafmt](https://scalameta.org/scalafmt/) configuration in [.scalafmt.conf](../blob/main/.scalafmt.conf).

The following command was used to fix the problem:
```
sbt scalafmtAll
```
