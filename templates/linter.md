Fixed code style violations with `sbt scalafmtAll`.

The default branch was pushed to with code that does not conform to the project's Scalafmt rules.
This pull request applies the required formatting fixes automatically.

### Trigger

This pull request was triggered by a push to the default branch that failed the linter check.
It is advised that formatting be fixed _before_ merging to the default branch.
